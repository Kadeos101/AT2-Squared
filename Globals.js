
class Question {
  
  constructor(x, y, w, h, C) {
    //Initializes a new Block with position (x, y), width, and height.
    this.Color = C;
    this.x = x; // Block's x-coordinate
    this.y = y; // Block's y-coordinate
    this.w = w; // Width of the Block
    this.h = h; // Height of the Block
  }
  
    }
    rect(this.x, this.y, this.w, this.h); // Draw the wall as a rectangle    
     

